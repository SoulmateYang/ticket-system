---
status: ACTIVE
---
# CEO Plan: 景区票务中台
Generated by /plan-ceo-review on 2026-05-26
Branch: main | Mode: SELECTIVE EXPANSION
Repo: SoulmateYang/ticketSystem

## Vision

### 10x Check
景区票务中台不只是"替代人工"——它是景区的数据控制权保卫战。通过统一管所有票种（年票/月票/次票/OTA分销）和统一核销，景区负责人第一次拥有了自己景区经营的"上帝视角"：用户画像、峰谷预测、渠道对比、实时核销数据。OTA平台化景区之前，这是最后的时间窗口。

### Platform Potential
年票/月票数据模型预留用户画像字段 → 未来可扩展为景区会员CRM系统（积分、等级、优惠套餐）。从"票务系统"到"景区CRM"的自然扩展路径。**但这是Phase 3+的规划，不在当前scope内。**

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | 数据模型预留用户画像和渠道分析字段 | S | ACCEPTED | 现在设计只是多几个字段，以后改schema成本天差地别 |
| 2 | 扫码枪即插即用 + 验票音效 | S | ACCEPTED | 最低成本、最高感知价值，直接提升验票员体验 |
| 3 | OTA订单自动同步 | M | ACCEPTED | 美团/抖音/携程订单自动进系统，财务不用手动导入Excel |
| 4 | 会员系统和积分体系字段预留 | S | ACCEPTED | Phase 1设计时预留几乎零成本，为未来扩展留口子 |

## Accepted Scope (added to this plan)

### Phase 1.5 Delight Items
- **扫码枪即插即用**：USB HID扫码枪插上自动识别，使用 `hidapi` 跨平台库监听设备插入/移除事件，不需要验票员配置
- **验票音效**：WAV格式音效，放在 `assets/sounds/` 目录，使用 `pygame` 或系统 `play` 命令播放，核销成功"滴"一声，失败"嘟嘟"两声

### Data Infrastructure
- **用户入场记录**：每次年票/月票入场留存时间戳，支持用户画像和峰谷分析
- **渠道分析字段**：订单来源标记（现场窗口/小程序/美团/抖音/携程），支持分销决策

### OTA Integration (Phase 2基础)
- **OTA订单自动同步**：美团/抖音/携程API订单自动同步进系统，无需人工Excel导入
- **前提条件**：OTA平台需开放API给第三方分销商。Phase 2启动前必须完成API可用性验证。

### Future Extensibility
- **会员系统和积分体系预留**：member_id, points_balance, tier_level字段预留（**Phase 3+功能，schema预留实现，Phase 2不实现**）

## Deferred to TODOS.md

- 人脸识别验票（Phase 2，需要硬件集成）
- 完整财务对账Excel导出（Phase 1.5）
- 员工权限管理详细设计（Phase 2）
- 景区会员CRM完整功能（Phase 3+）

## Technical Architecture Notes

### Unified Ticket Model
```
TicketType:
  - type: YEAR_PASS | MONTH_PASS | SINGLE_USE | OTA_TICKET
  - visitor_id: 身份证号（年票/月票）
  - face_template: 人脸特征（**Phase 2实现，schema预留字段**）
  - valid_from, valid_to: 有效期
  - max_entries: 最大入园次数（年票/月票）
  - max_entries_exceeded_action: REJECT | WARN（超次处理策略）

EntryLog:
  - ticket_id, visitor_id, entry_time, entry_type
  - channel: WINDOW | MINIAPP | MEITUAN | DOUYIN | CTRIP
  - verified_by: STAFF_ID

OTAOrder:
  - ota_channel, external_order_id
  - synced_to_system: true/false
  - sync_time, sync_status
```

### Core Modules (Phase 1 Priority Order)
1. **年票/月票管理**（核心护城河，Phase 1先做）
2. **QR码次票核销**（技术成熟，并行开发）
3. **扫码枪即插即用 + 音效**（Phase 1.5，低成本高体验）
4. **OTA订单自动同步**（Phase 2基础，Phase 1预留接口）
5. **统一数据模型 + 用户入场记录**（支撑后续分析能力）

## Acceptance Criteria & Timeline

### Phase 1: 年票/月票 + 次票核销（目标：旺季前上线）

| 功能 | 完成标准 | 预计周期 |
|------|---------|---------|
| 年票/月票CRUD | 管理员可创建/激活/暂停/取消年票月票，身份证绑定，支持有效期校验 | 2周 |
| 次票QR码核销 | 扫码枪扫描后2秒内返回核销结果，声音提示，防重复验票 | 1周 |
| 扫码枪即插即用 | USB HID扫码枪插入后自动识别，无需配置 | 2天 |
| 验票音效 | 核销成功/失败不同音效，验票员无需盯屏幕 | 1天 |
| 用户入场记录 | 每次入场留存记录，支持按时间/渠道查询 | 3天 |
| 统一数据模型 | TicketType支持YEAR_PASS/MONTH_PASS/SINGLE_USE/OTA_TICKET四种类型 | 1周 |

**Phase 1 技术验收标准**：
- 年票/月票入场核销成功率 > 99.9%（失败即拒绝入场）
- 次票QR码核销延迟 < 2秒
- 验票员满意度：不用盯着屏幕看结果，听声音就知道

### Phase 2: OTA对接 + 高级功能

| 功能 | 完成标准 | 前置条件 |
|------|---------|---------|
| OTA订单自动同步 | 美团/抖音/携程订单自动进系统，无人工导入 | OTA API验证通过 |
| 人脸识别验票 | 准确率≥99.5%，支持离线场景 | 硬件供应商确定 |
| 员工权限管理 | 基于角色的访问控制，验票员只能验票 | Phase 1稳定运行 |

**Phase 2 前提**：OTA平台API可用性必须先验证。当前设计方案预留了接口，如果API不可用需要切换到手动导入模式。