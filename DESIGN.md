# Design: 景区票务系统设计规范

Generated by /office-hours on 2026-05-26
Updated with Cyberpunk UI System on 2026-05-26
Status: IMPLEMENTED
Mode: Production

---

## 1. Concept & Vision

赛博朋克暗调科技风 — 深海蓝黑底配霓虹青高光，致敬经典Terminal美学。界面呈现"数字操控台"质感：深邃背景、清晰网格、精炼信息、霓虹点缀。工具感强、沉浸感足，适合专业人员高频操作的管理后台。

---

## 2. Design Language

### 2.1 Color Palette

```css
/* 背景层级 */
--bg-main:     #030811;   /* 主背景：极深蓝黑 */
--bg-secondary:#0A1628;   /* 次级背景：深蓝 */
--bg-card:    #0D1F35;   /* 卡片/表格背景 */
--bg-hover:   #0F2847;   /* 悬停态 */
--bg-selected:#164D7A;    /* 选中态 */

/* 霓虹强调色 */
--neon-cyan:     #00F5FF;  /* 主强调色：霓虹青 */
--neon-cyan-glow:rgba(0, 245, 255, 0.4);
--neon-cyan-dim: rgba(0, 245, 255, 0.15);

/* 按钮 */
--btn-primary:     #00F5FF;
--btn-primary-hover:  #5FFFFF;
--btn-primary-active: #00C4CC;
--btn-secondary:   #0F2847;

/* 文字 */
--text-primary:   #FFFFFF;
--text-secondary: #7EB8DA;
--text-muted:     #3D5A73;

/* 边框 */
--border:      #1E3A5F;
--border-light:rgba(30, 58, 95, 0.5);

/* 状态色 */
--success: #00FF88;   --success-glow: rgba(0, 255, 136, 0.3);
--warning: #FF9500;   --warning-glow: rgba(255, 149, 0, 0.3);
--danger:  #FF2D6A;   --danger-glow:  rgba(255, 45, 106, 0.3);
```

### 2.2 Typography

| 用途 | 字体 | 尺寸 | 字重 | 字间距 |
|------|------|------|------|--------|
| 页面标题 | Noto Serif SC | 28px | 700 | 0.1em |
| 副标题/标签 | Noto Serif SC | 11px | 400 | 0.3em |
| 正文 | Noto Serif SC | 14px | 400 | 0.05em |
| 表头 | Noto Serif SC | 12px | 600 | 0.1em |
| 单行代码/ID | JetBrains Mono | 13px | 400 | - |

### 2.3 Spacing System

基础单位：8px

| Token | 值 | 用途 |
|-------|----|------|
| `--space-xs` | 4px | 标签内边距 |
| `--space-sm` | 8px | 组件内间距 |
| `--space-md` | 12-14px | 输入框内边距 |
| `--space-lg` | 16-20px | 单元格内边距 |
| `--space-xl` | 24px | 面板间距 |
| `--space-2xl` | 32-48px | 页面边距 |

### 2.4 Motion Philosophy

- **进入动画**：淡入 + 微上移 (0.2-0.3s ease-out)，表格行延迟叠加 (index * 0.05s)
- **悬停反馈**：背景色过渡 0.2s，无缩放
- **按钮反馈**：translateY(-2px) + 颜色变化
- **对话框**：缩放 0.95→1 + 透明度 0→1 (0.3s ease)

---

## 3. Component Inventory

### 3.1 Page Header

```
padding: 32px 48px 24px
背景: var(--bg-secondary)
下边框: 1px solid var(--border)
结构: 标题区(左) + 操作区(右)
```

### 3.2 Search Panel

```
padding: 24px 48px
背景: var(--bg-secondary)
下边框: 1px solid var(--border)
结构: 表单项(左) + 按钮(右，margin-left: auto)
```

### 3.3 Data Table

```
表头背景: var(--bg-secondary)
表头文字: var(--text-secondary), 12px, 600
单元格padding: 16px 48px
行悬停: var(--bg-hover)
下边框: 1px solid var(--border)
```

### 3.4 Status Badges

| 状态 | 背景色 | 文字色 |
|------|--------|--------|
| 待处理/待激活 PENDING | --warning | --bg-main |
| 有效/已完成 ACTIVE/SYNCED | --success | --bg-main |
| 已挂失/已注销 SUSPENDED/CANCELLED | --danger | white |
| 单次票 SINGLE_USE | --warning | --bg-main |

### 3.5 Type Badges

| 类型 | 背景色 |
|------|--------|
| 年卡 YEAR_PASS | --danger |
| 月卡 MONTH_PASS | --success |

### 3.6 Action Buttons

```
主要操作: 背景 --accent, 文字 --bg-main
次要操作: 背景 --btn-secondary, 边框 --border
危险操作: 背景 --danger, 文字 white
悬停: translateY(-1px), opacity 0.9
禁用: opacity 0.5, cursor not-allowed
```

### 3.7 Dialog

```
遮罩: rgba(0, 15, 30, 0.7)
背景: var(--bg-card)
边框: 1px solid var(--border)
圆角: 8px
动画: dialog-enter 0.3s ease
```

### 3.8 Pagination

```
位置: 底部固定
padding: 20px 48px
背景: var(--bg-secondary)
上边框: 1px solid var(--border)
```

---

## 4. Implemented Views

| 视图 | 路径 | 状态 |
|------|------|------|
| Dashboard | `/dashboard` | ✅ 自定义Cyberpunk CSS |
| 票据列表 | `/tickets` | ✅ 自定义Cyberpunk CSS |
| 年/月卡管理 | `/tickets/passes` | ✅ 自定义Cyberpunk CSS |
| OTA订单 | `/ota/orders` | ✅ 自定义Cyberpunk CSS |
| 入园记录 | `/logs/entries` | ✅ 自定义Cyberpunk CSS (已修复) |
| 系统设置 | `/settings` | ⬜ 待实现 |

---

## 5. Technical Notes

### 5.1 CSS Architecture

所有视图使用 `<style scoped>` 内的 CSS Variables 定义色板，确保样式隔离。共用变量定义在 `AdminLayout.vue` 中通过继承或重复定义实现一致性。

**关键约束**：不得在 EntryLogs 以外的任何视图使用 Element Plus 组件（el-card, el-table, el-pagination 等）。所有列表页必须使用原生 HTML table + 自定义 CSS。

### 5.2 Icon Strategy

使用内联 SVG（viewBox: 0 0 24 24, stroke-width: 2），禁止使用第三方图标库以保持包体积最小化。

### 5.3 Font Loading

```html
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600&family=Noto+Serif+SC:wght@400;600;700&display=swap" rel="stylesheet">
```

---

## 6. Open Questions

1. **系统设置页面** — 尚未实现UI设计
2. **响应式布局** — 当前为大屏优化，移动端适配待定
3. **暗色主题切换** — 暂无亮色主题计划
4. **人脸识别UI** — Phase 2功能，UI待设计